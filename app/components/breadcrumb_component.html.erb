<nav class="p-4 bg-gray-50">
  <ol class="flex flex-row text-gray-500">
    <% each do |link| %>
      <li class="mr-2 after:content-['/'] after:inline-block after:ml-2 after:last:content-['']"><%= link_to_unless link.current?, link.text, link.url, class: 'text-red-500 hover:text-red-600' %></li>
    <% end %>
  </ol>
</nav>
