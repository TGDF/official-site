section#partners
  .container
    .sec-title.text-center.mb-4
      h1.heading = sanitize t('.heading')
    .partners
      .row
        - @partners_and_sponsors.each do |partner|
          .col-md-2.col-sm-4.col-xs-12
            .card.partner
              = link_to image_tag(partner.logo_url(:v1)), partner.url, target: '_blank'
