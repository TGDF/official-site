<%# Admin Partner Types Edit - TailwindCSS v2 variant %>
<div class="space-y-6">
  <div class="mb-6">
    <h1 class="text-xl font-semibold text-gray-900 mb-2"><%= t('.edit') %></h1>
    <p class="text-gray-600 text-sm"><%= t('.description', default: 'Edit partner type') %></p>
  </div>

  <div class="bg-white border border-gray-200 rounded-md shadow-sm overflow-hidden">
    <div class="p-6">
      <%= render 'form' %>
    </div>
  </div>
</div>