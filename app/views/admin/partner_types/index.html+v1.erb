<%# CONVERTED FROM SLIM - NEEDS MANUAL ERB CONVERSION %>
= admin_page_header t('.types'), t('.manager_partner_types')
.container-fluid
  .fade-in
    .row
      .col-md-12
        = admin_box do
          = admin_box_body class: 'no-padding' do
            table.table.table-striped
              thead
                tr
                  th.table__id-column #
                  th = t('.name')
                  th = t('.order')
                  th.col-md-3 = t('.actions')
              tbody
                = render @types

