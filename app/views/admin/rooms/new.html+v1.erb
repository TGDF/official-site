<%# CONVERTED FROM SLIM - NEEDS MANUAL ERB CONVERSION %>
= admin_page_header t('.create'), t('.add_room')
.container-fluid
  .row
    .col-md-12
      = admin_box do
        = admin_box_header t('.create'), class: 'with-border'
        = render 'form'
