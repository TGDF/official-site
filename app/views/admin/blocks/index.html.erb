<%= admin_page_header t('.blocks'), t('.manage_blocks') %>
<div class="container-fluid">
  <div class="fade-in">
    <div class="row">
      <div class="col-md-12">
        <%= admin_box do %>
          <%= admin_box_header title t('.blocks') %>
          <%= admin_box_body do %>
            <table class="table table-striped">
              <thead>
                <tr>
                  <th class="table__id-column">#</th>
                  <th><%= t('.content') %></th>
                  <th><%= t('.language') %></th>
                  <th><%= t('.page') %></th>
                  <th><%= t('.actions') %></th>
                </tr>
              </thead>
              <tbody>
                <%= render @blocks %>
              </tbody>
            </table>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>
