<%# Admin Sliders New - TailwindCSS v2 variant %>
<div class="space-y-6">

  <div class="bg-white border border-gray-200 rounded-md shadow-sm overflow-hidden">
    <div class="p-6">
      <div class="mb-6">
        <h3 class="text-base font-medium text-gray-900"><%= t('.create') %></h3>
        <p class="text-gray-600 text-sm"><%= t('.add_slider', default: 'Create a new slider') %></p>
      </div>
      <%= render 'form' %>
    </div>
  </div>
</div>
