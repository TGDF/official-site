<%# Admin Agenda Times New - TailwindCSS v2 variant %>
<div class="space-y-6">
  <div class="mb-6">
    <h1 class="text-xl font-semibold text-gray-900 mb-2"><%= t('.create') %></h1>
    <p class="text-gray-600 text-sm"><%= t('.description', default: 'Create a new agenda time') %></p>
  </div>

  <div class="bg-white border border-gray-200 rounded-md shadow-sm overflow-hidden">
    <div class="p-6">
      <%= render 'form' %>
    </div>
  </div>
</div>