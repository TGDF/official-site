<%# Admin Sites Edit - TailwindCSS v2 variant %>
<div class="space-y-6">

  <%= admin_v2_locale_navtab %>

  <div class="bg-white border border-gray-200 rounded-md shadow-xs overflow-hidden">
    <div class="p-6 space-y-6">
      <div class="mb-6">
        <h3 class="text-base font-medium text-gray-900"><%= t('.edit_site', default: 'Edit Site') %></h3>
        <p class="text-gray-600 text-sm"><%= t('.edit_exists_site', default: 'Update site information and settings') %></p>
      </div>
      <%= render 'form' %>
    </div>
  </div>
</div>
