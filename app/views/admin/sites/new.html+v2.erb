<%# Admin Sites New - TailwindCSS v2 variant %>
<div class="space-y-6">
  <header class="mb-6">
    <h1 class="text-lg font-bold text-admin-text-primary"><%= t('.create') %></h1>
    <p class="text-sm text-admin-text-secondary mt-1"><%= t('.description', default: 'Create a new site') %></p>
  </header>
  
  <div class="bg-admin-background rounded-admin-md shadow-admin-sm border border-admin-border p-4">
    <div class="p-4 space-y-4">
      <%= render 'form' %>
    </div>
  </div>
</div>
