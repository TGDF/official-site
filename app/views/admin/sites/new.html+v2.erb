<%# Admin Sites New - TailwindCSS v2 variant %>
<div class="space-y-6">
  <header class="mb-6">
    <h1 class="text-2xl font-semibold text-gray-900 mb-2"><%= t('.create') %></h1>
    <p class="text-gray-600"><%= t('.description', default: 'Create a new site') %></p>
  </header>
  
  <div class="bg-white rounded-lg shadow border border-gray-200 overflow-hidden">
    <div class="p-6">
      <%= render 'form' %>
    </div>
  </div>
</div>
