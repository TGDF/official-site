<%# Admin Sites Edit - TailwindCSS v2 variant %>
<div class="space-y-6">
  <header class="mb-6">
    <h1 class="text-2xl font-semibold text-gray-900 mb-2"><%= t('.edit') %></h1>
    <p class="text-gray-600"><%= t('.description', default: 'Edit site details') %></p>
  </header>
  
  <div class="bg-white rounded-lg shadow border border-gray-200 overflow-hidden">
    <div class="p-6 space-y-6">
      <div class="mb-4 border-b border-gray-200 pb-2">
        <%= admin_locale_navtab %>
      </div>
      <%= render 'form' %>
    </div>
  </div>
</div>
