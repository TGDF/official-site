<%# Admin Sites Edit - TailwindCSS v2 variant %>
<div class="space-y-6">
  <header class="mb-6">
    <h1 class="text-lg font-bold text-admin-text-primary"><%= t('.edit') %></h1>
    <p class="text-sm text-admin-text-secondary mt-1"><%= t('.description', default: 'Edit site details') %></p>
  </header>
  
  <div class="bg-admin-background rounded-admin-md shadow-admin-sm border border-admin-border">
    <div class="p-4 space-y-4">
      <div class="mb-4 border-b border-admin-border pb-2">
        <%= admin_locale_navtab %>
      </div>
      <%= render 'form' %>
    </div>
  </div>
</div>
