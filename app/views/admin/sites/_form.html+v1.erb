<%# CONVERTED FROM SLIM - NEEDS MANUAL ERB CONVERSION %>
- Mobility.with_locale(admin_current_resource_locale) do
  = simple_form_for [:admin, @site] do |f|
    = admin_box_body do
      / TODO: Upgrade mobility to support jsonb in form
      = f.input :name, as: :string
      = f.input :domain
      = f.input :logo, as: :file
      = f.input :tenant_name if @site.new_record?
      = f.input :description, as: :text, input_html: { data: { editor: true }, rows: 20 }
      = f.input :figure, as: :file
    = admin_box_footer do
      = f.submit class: 'btn btn-primary'
