<%# CONVERTED FROM SLIM - NEEDS MANUAL ERB CONVERSION %>
tr
  td = sponsor.id
  td = sponsor.name
  td = sponsor.level.name
  td = sponsor.order
  td
    .d-flex.align-items-start
      = link_to t('.edit'), edit_admin_sponsor_path(sponsor), class: 'btn btn-primary'
      / TODO: Create helper
      = button_to t('.destroy'), admin_sponsor_path(sponsor), class: 'btn btn-danger', method: :delete, data: { confirm: t('shared.destroy_confirm') }
