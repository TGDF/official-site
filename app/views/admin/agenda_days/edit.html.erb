<%# Admin Agenda Days Edit - TailwindCSS v2 variant %>
<div class="space-y-6">
  <div class="bg-white border border-gray-200 rounded-md shadow-xs overflow-hidden">
    <div class="p-6">
      <div class="mb-6">
        <h3 class="text-base font-medium text-gray-900"><%= t('.edit') %></h3>
        <p class="text-gray-600 text-sm"><%= t('.description', default: 'Edit agenda day') %></p>
      </div>
      <%= render 'form' %>
    </div>
  </div>
</div>
