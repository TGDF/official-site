<%# CONVERTED FROM SLIM - NEEDS MANUAL ERB CONVERSION %>
= admin_page_header t('.edit'), t('.edit_day')
.container-fluid
  .row
    .col-md-12
      = admin_box do
        = admin_box_header t('.edit'), class: 'with-border'
        = render 'form'
