<%# CONVERTED FROM SLIM - NEEDS MANUAL ERB CONVERSION %>
= simple_form_for current_admin_user, url: admin_profile_path(current_admin_user) do |f|
  = admin_box_body do
    = f.input :password
    = f.input :password_confirmation
  = admin_box_footer do
    = f.submit class: 'btn btn-primary'
