<%# Admin Profile Edit - TailwindCSS v2 variant %>

<div class="space-y-6">
  <div class="mb-6">
    <h1 class="text-xl font-semibold text-gray-900 mb-2"><%= t('.user', default: 'User Profile') %></h1>
    <p class="text-gray-600 text-sm">
      <%= t('.edit_profile', default: 'Edit your profile information and account settings') %>
    </p>
  </div>

  <div class="bg-white border border-gray-200 rounded-md shadow-xs">
    <div class="p-6">
      <%= render 'form' %>
    </div>
  </div>
</div>